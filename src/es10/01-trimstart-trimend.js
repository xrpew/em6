const hello = ' .        hola como esta s ';
console.log(hello.length)
console.log(hello.trimStart().length)
console.log(hello.trimStart().length)
console.log(hello.trim().length)

const trimeado = hello.trim()
console.log(trimeado.length)